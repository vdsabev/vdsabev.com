{
  "private": true,
  "name": "vdsabev.com",
  "scripts": {
    "build": "cmless build && node copy _redirects ./build/_redirects",
    "build:assets": "cd src && svgo avatar.svg && svg2png avatar.svg -w 100",
    "profile": "cmless profile",
    "format": "cmless format",
    "start": "cmless start",
    "deploy": "npm run build && firebase deploy",
    "deploy:functions": "firebase deploy --only functions",
    "deploy:hosting": "npm run build && firebase deploy --only hosting"
  },
  "dependencies": {
    "@hyperapp/router": "0.4.1",
    "classcat": "3.0.2",
    "overstate": "1.1.0",
    "tslib": "1.9.0",
    "ultradom": "2.0.0"
  },
  "devDependencies": {
    "cmless": "0.4.0",
    "firemail": "1.5.0"
  },
  "cmless": {
    "input": "src",
    "script": "src/index.ts",
    "style": "src/style.ts"
  }
}
